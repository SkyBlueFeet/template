export { userTableconfig, elementTableConfig, moduleTableConfig, roleTableConfig } from './table';
export { elementFormConfig, moduleFormConfig, roleFormConfig, userFormConfig } from './form';

export { default as template } from './elem.template';

export const Router = {
    ignoreExecute: [''],
    defaultPage: '/page/app',
    loginPage: '/'
};