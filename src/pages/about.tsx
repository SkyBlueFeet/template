import React, { Component } from "react";
import VHeader from "../components/header";

export default class About extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <VHeader text="About Page" />;
  }
}
